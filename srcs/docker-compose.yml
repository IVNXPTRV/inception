version: '3.8'

services:
  nginx:
    container_name: nginx
    build: ./requirements/nginx/
    ports:
      - 80:80
      - 443:443
    restart: always
    secrets:
      - server.key
    
secrets:
  server.key:
    file: ../secrets/server.key

